---
title: "R Notebook"
output: html_notebook
---
```{r}
library(devtools)
library(tidyverse)
load_all(".")
source("./R/NUTS.R")
```
```{r}
library(roxygen2); # Read in the roxygen2 R package
roxygenise(); 
```




# Loading bridgestan model

```{r}

library(rstan)

model_name <- "gaussian"
bridgestan_path <- "~/Documents/bridgestan"



model <- get_model(model_name)

```


```{r}
sampling(model$stan_model, model$data) 
```


```{r}
samples <- NUTS(model$BS_model, c(0,1), 0.001, 1000)
```


```{r}
data= get_mixture_data(100, pi=c(0.7, 0.3), mu=matrix(c(-5, 2), 2, 1), sigma = rep(1,2))
```


```{r}
model_name <- "gaussian_mixture"
bridgestan_path <- "~/Documents/bridgestan"

write_json_data(data, bridgestan_path, model_name)
```



